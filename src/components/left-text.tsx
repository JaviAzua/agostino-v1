import { cn } from "@/lib/utils";
import React from "react";

export default function LeftText({ className }: { className?: string }) {
  return <p className={cn(className)}>vision into visuals</p>;
}
